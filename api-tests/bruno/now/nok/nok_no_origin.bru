meta {
  name: nok_no_origin
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/now
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
  res.body: isDefined
  res.body.message: isDefined
  res.body.message: matches {{CORS_REGEX}}
  res.responseTime: lt 200
}
