{
  "name": "beavuck-time",
  "version": "2.1.2",
  "main": "build/api.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "run-integration-tests": "npm install -g newman && newman run api-tests/collection.json -e api-tests/environment.json --bail",
    "up-patch": "npm version patch --no-git-tag-version",
    "up-minor": "npm version minor --no-git-tag-version",
    "up-major": "npm version major --no-git-tag-version",
    "update-dependencies": "ncu --format group && ncu -u && npm install && npm prune && npm audit fix",
    "test": "DOTENV_CONFIG_PATH=./.env.test jest --coverage",
    "build": "tsoa spec-and-routes && tsc",
    "start": "node build/server.js",
    "go": "npm run build && npm run test && npm start"
  },
  "keywords": [],
  "author": "Alexis Drai",
  "license": "Unlicense",
  "private": "false",
  "description": "Get time in ISO format, in UTC timezone, from a simple, lightweight node server",
  "homepage": "https://gitlab.com/beavuck-services/time",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/beavuck-services/time.git"
  },
  "bugs": "https://gitlab.com/beavuck-services/time/-/issues",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.0.0",
    "express-rate-limit": "^7.4.1",
    "http-status-codes": "^2.3.0",
    "joi": "^17.13.3",
    "tsoa": "^6.5.1",
    "winston": "^3.16.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.8.6",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "npm-check-updates": "^17.1.9",
    "prettier": "3.3.3",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
